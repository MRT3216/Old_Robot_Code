The program writes a series of comma separated values to a text file, which can then be opened in Excel and graphes.  We could use this information to analyze games afterwards and visualize what happened.  It could store information such as framerate vs byte[] size or vision processing datapoints.  The flush() and close() commands should be in the same method as shutdown() with a delay between flushing and closing and shutting down to accurately store the information.